import axiosInstance from './axiosInstance'
import { ENDPOINTS } from '../constants'
// Owner: Quốc — Reception APIs (UC-26→43)
export const bookingService = {
  // TODO(Quốc): listBookings, getDetail, walkIn, checkIn, checkOut, addExtraService, bill,
  //   cancel, noShow, transfer, roomSchedule, transactions ...
}
