import axiosInstance from './axiosInstance'
import { ENDPOINTS } from '../constants'
// Owner: Tú — Housekeeper APIs (UC-44→55)
export const taskService = {
  // TODO(Tú): listTasks, getDetail, claim, start, complete, saveAmenityReport, reportIssue ...
}
