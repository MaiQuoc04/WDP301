import { createSlice } from '@reduxjs/toolkit'
// Owner: Sáng — Super Admin state (UC-71→82)
const adminSlice = createSlice({
  name: 'admin',
  initialState: {},
  reducers: {},
})
export default adminSlice.reducer
