import { createSlice } from '@reduxjs/toolkit'
// Owner: Tú — Housekeeping state (UC-44→55)
const taskSlice = createSlice({
  name: 'task',
  initialState: {},
  reducers: {},
})
export default taskSlice.reducer
