import { createSlice } from '@reduxjs/toolkit'
// Owner: Khánh — Guest/Customer state (UC-01→25)
const customerSlice = createSlice({
  name: 'customer',
  initialState: {},
  reducers: {},
})
export default customerSlice.reducer
