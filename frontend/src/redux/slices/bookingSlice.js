import { createSlice } from '@reduxjs/toolkit'
// Owner: Quốc — Reception/Booking state (UC-26→43)
const bookingSlice = createSlice({
  name: 'booking',
  initialState: {},
  reducers: {},
})
export default bookingSlice.reducer
