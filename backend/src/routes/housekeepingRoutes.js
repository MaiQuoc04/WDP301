// Owner: Tú — Housekeeper APIs (UC-44→55)
const router = require('express').Router()
// const { protect, authorize } = require('../middlewares/authMiddleware')
// router.use(protect, authorize('housekeeper'))

// TODO(Tú): tasks list/detail/filter, claim/start/complete, check amenities + report,
//   report issue, mark maintenance, completed history

module.exports = router
