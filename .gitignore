node_modules/
.env
.DS_Store
dist/

# Tài liệu nặng — để trên Google Drive (theo SPMP), không đưa vào repo
WDP_Report_Final.docx
*.docx
_docx_extract/

# stray root lock (chỉ root — không có package.json gốc; backend/frontend lock vẫn track)
/package-lock.json

# Stitch MCP config chứa API key thật, không commit
/.mcp.json
